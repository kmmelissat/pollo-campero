apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: pollos
spec:
  selector:
    app: order-service
  ports:
    - port: 3002
      targetPort: 3002
      nodePort: 30002
  type: NodePort